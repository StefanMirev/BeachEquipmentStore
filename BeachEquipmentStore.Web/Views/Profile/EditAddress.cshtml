@model AddressViewModel

<h1>Редакция на адрес</h1>

<nav class="col-md-3 col-lg-2 d-md-block bg-light sidebar">
    <partial name="_ManageNav" />
</nav>
<form method="post">
    <div class="form-group">
        <label for="name">Адрес</label>
        <input type="text" class="form-control" id="name" name="name" value="@Model.Name" required>
    </div>
    <div class="form-group">
        <label for="town">Град</label>
        <input type="text" class="form-control" id="town" name="town" value="@Model.Town" required>
    </div>
    <div class="form-group">
        <label for="zipCode">Пощенски код</label>
        <input type="text" class="form-control" id="zipCode" name="ZipCode" value="@Model.ZipCode" required>
    </div>
    <button type="submit" class="btn btn-primary" asp-route-userId="@User.GetId()" asp-route-addressId="@Model.Id">Запази промените</button>
</form>

<a asp-controller="Profile" asp-action="GetAddress" asp-route-userId="@User.GetId()">Откажи промените</a>