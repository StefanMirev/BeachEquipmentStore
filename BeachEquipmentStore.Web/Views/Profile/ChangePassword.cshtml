@model ChangePasswordViewModel

@{
    ViewData["Title"] = "Промяна на парола";
}

<h2>@ViewData["Title"]</h2>
<hr />

    @if (!string.IsNullOrEmpty((string?)ViewData["ErrorMessage"]))
    {
        <div class="alert alert-danger mt-5">
            @TempData["ErrorMessage"]
        </div>
    }

<div class="row">
    <nav class="col-md-3 col-lg-2 d-md-block bg-light sidebar">
        <partial name="_ManageNav" />
    </nav>
    <div role="main" class="col-md-9 ml-sm-auto col-lg-10 px-md-4">
        <form method="post">
            <div class="form-group">
                <label asp-for="CurrentPassword">Текуща парола</label>
                <input asp-for="CurrentPassword" class="form-control" required />
            </div>

            <div class="form-group">
                <label asp-for="NewPassword">Нова парола</label>
                <input asp-for="NewPassword" class="form-control" required />
            </div>

            <div class="form-group">
                <label asp-for="ConfirmPassword">Потвърди парола</label>
                <input asp-for="ConfirmPassword" class="form-control" required />
            </div>
            <button style="margin-top: 5px;" type="submit" class="btn btn-primary">Запази промените</button>
        </form>
    </div>
</div>
